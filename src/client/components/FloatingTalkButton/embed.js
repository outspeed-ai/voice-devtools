import FloatingTalkButton from './FloatingTalkButton';

// // Create a function to mount your component to a DOM element
// window.initOutspeed = (elementId, props = {}) => {
//   const container = document.getElementById(elementId)
//   if (container) {
//     ReactDOM.render(React.createElement(, props), container)
//   }
// }

// Optionally expose your component directly
window.FloatingTalkButton = FloatingTalkButton